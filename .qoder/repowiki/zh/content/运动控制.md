# 运动控制系统技术文档

<cite>
**本文档引用的文件**
- [lcd_rom_pic.v](file://lcd_rom_pic.v)
- [lcd_display.v](file://lcd_display.v)
- [lcd_driver.v](file://lcd_driver.v)
- [pic_rom.v](file://pic_rom.v)
- [lcd_pll.v](file://lcd_pll.v)
- [CrazyBird.mif](file://CrazyBird.mif)
- [miqi.mif](file://miqi.mif)
- [picture1.mif](file://picture1.mif)
- [lcd_display.txt](file://lcd_display.txt)
- [lcd_driver.txt](file://lcd_driver.txt)
</cite>

## 目录
1. [项目概述](#项目概述)
2. [系统架构](#系统架构)
3. [核心组件分析](#核心组件分析)
4. [运动控制算法详解](#运动控制算法详解)
5. [显示模式实现原理](#显示模式实现原理)
6. [速度控制机制](#速度控制机制)
7. [边界处理逻辑](#边界处理逻辑)
8. [拨码开关输入处理](#拨码开关输入处理)
9. [定时器配置与坐标变换](#定时器配置与坐标变换)
10. [状态机设计与时序分析](#状态机设计与时序分析)
11. [性能优化建议](#性能优化建议)
12. [故障排除指南](#故障排除指南)

## 项目概述

本项目是一个基于FPGA的运动控制系统，实现了四种不同的显示模式：静态显示、垂直振荡、水平移动和对角移动。系统采用模块化设计，通过拨码开关实现模式选择，支持精确的速度控制和边界处理逻辑。

## 系统架构

系统采用分层架构设计，主要包含以下层次：

```mermaid
graph TB
subgraph "应用层"
UI[用户界面<br/>拨码开关输入]
end
subgraph "控制层"
CTRL[运动控制器<br/>状态机控制]
SPEED[速度控制器<br/>节拍生成]
end
subgraph "显示层"
DISPLAY[显示控制器<br/>图像渲染]
COORD[坐标转换器<br/>像素坐标计算]
end
subgraph "硬件层"
DRIVER[LCD驱动器<br/>时序控制]
PLL[时钟管理器<br/>频率合成]
ROM[图像存储器<br/>MIF文件加载]
end
UI --> CTRL
CTRL --> DISPLAY
SPEED --> CTRL
DISPLAY --> COORD
COORD --> DRIVER
DRIVER --> PLL
DISPLAY --> ROM
```

**图表来源**
- [lcd_rom_pic.v:1-59](file://lcd_rom_pic.v#L1-L59)
- [lcd_display.v:1-199](file://lcd_display.v#L1-L199)
- [lcd_driver.v:1-97](file://lcd_driver.v#L1-L97)

## 核心组件分析

### 主控制器模块

主控制器模块负责协调整个系统的运行，包含时钟管理和模块间通信。

```mermaid
classDiagram
class LcdRomPic {
+input sys_clk
+input sys_rst_n
+input B0-B3
+output lcd_hs
+output lcd_vs
+output lcd_de
+output pixel_data
+assign rst_n_w
+lcd_pll u_lcd_pll
+lcd_driver u_lcd_driver
+lcd_display u_lcd_display
}
class LcdDisplay {
+input lcd_clk
+input sys_rst_n
+input B0-B3
+input pixel_xpos
+input pixel_ypos
+output pixel_data
+parameter H_DISP,V_DISP
+localparam WIDTH,HEIGHT,TOTAL
+reg move_cnt
+reg move_en
+reg img_x,img_y
+reg x_dir,y_dir
}
class LcdDriver {
+input lcd_clk
+input sys_rst_n
+output lcd_hs,lcd_vs
+output lcd_de
+output pixel_xpos
+output pixel_ypos
+parameter H_SYNC,H_DISP,V_SYNC,V_DISP
+reg cnt_h,cnt_v
}
LcdRomPic --> LcdDisplay
LcdRomPic --> LcdDriver
LcdDisplay --> LcdDriver
```

**图表来源**
- [lcd_rom_pic.v:1-59](file://lcd_rom_pic.v#L1-L59)
- [lcd_display.v:1-199](file://lcd_display.v#L1-L199)
- [lcd_driver.v:1-97](file://lcd_driver.v#L1-L97)

**章节来源**
- [lcd_rom_pic.v:1-59](file://lcd_rom_pic.v#L1-L59)
- [lcd_display.v:1-199](file://lcd_display.v#L1-L199)
- [lcd_driver.v:1-97](file://lcd_driver.v#L1-L97)

## 运动控制算法详解

### 运动节拍生成算法

系统使用计数器实现精确的运动节拍控制：

```mermaid
flowchart TD
Start([系统启动]) --> InitCount["初始化move_cnt=0<br/>move_en=0"]
InitCount --> WaitCycle["等待时钟周期"]
WaitCycle --> CheckMax{"move_cnt == MOVE_CNT_MAX-1?"}
CheckMax --> |是| ResetCount["move_cnt = 0<br/>move_en = 1"]
CheckMax --> |否| IncCount["move_cnt = move_cnt + 1<br/>move_en = 0"]
ResetCount --> GeneratePulse["生成运动脉冲"]
IncCount --> WaitCycle
GeneratePulse --> MoveObject["执行运动操作"]
MoveObject --> WaitCycle
```

**图表来源**
- [lcd_display.v:33-46](file://lcd_display.v#L33-L46)

### 运动参数计算

系统采用参数化设计，支持灵活的运动参数配置：

- **基础位置参数**：POS_X_BASE = 230, POS_Y_BASE = 150
- **图像尺寸参数**：WIDTH = 140, HEIGHT = 170, TOTAL = 23800
- **速度控制参数**：MOVE_CNT_MAX = 555,000 (对应60 px/s)
- **颜色参数**：YELLOW = 0xFFFF00, RED = 0xFF0000, WHITE = 0xFFFFFF

**章节来源**
- [lcd_display.v:10-28](file://lcd_display.v#L10-L28)

## 显示模式实现原理

### 静态显示模式

静态显示模式是最简单的模式，图像保持在初始位置不变。

```mermaid
sequenceDiagram
participant Switch as 拨码开关
participant Controller as 运动控制器
participant Display as 显示模块
Switch->>Controller : B0=1,B1=0,B2=0,B3=0
Controller->>Controller : casez匹配
Controller->>Display : 设置img_x=230<br/>img_y=150
Controller->>Display : 重置方向x_dir=0<br/>y_dir=0
Display->>Display : 静态渲染图像
```

**图表来源**
- [lcd_display.v:109-114](file://lcd_display.v#L109-L114)

### 垂直振荡模式

垂直振荡模式实现图像在垂直方向上的往复运动，距离为130像素。

```mermaid
flowchart TD
Start([开始垂直振荡]) --> CheckDirection{"检查运动方向"}
CheckDirection --> |向下| CheckBottom{"img_y >= POS_Y_BASE + 130?"}
CheckDirection --> |向上| CheckTop{"img_y <= POS_Y_BASE?"}
CheckBottom --> |是| ReverseDown["y_dir = 1<br/>img_y = img_y - 1"]
CheckBottom --> |否| MoveDown["img_y = img_y + 1"]
CheckTop --> |是| ReverseUp["y_dir = 0<br/>img_x = POS_X_BASE"]
CheckTop --> |否| MoveUp["img_y = img_y - 1"]
ReverseDown --> CheckDirection
MoveDown --> CheckDirection
ReverseUp --> CheckDirection
MoveUp --> CheckDirection
```

**图表来源**
- [lcd_display.v:88-107](file://lcd_display.v#L88-L107)

### 水平移动模式

水平移动模式实现图像在水平方向上的连续移动，遇到边界时反弹。

```mermaid
flowchart TD
Start([开始水平移动]) --> CheckDirection{"检查x_dir方向"}
CheckDirection --> |向右| CheckRight{"img_x >= H_DISP - WIDTH?"}
CheckDirection --> |向左| CheckLeft{"img_x <= 0?"}
CheckRight --> |是| ReverseRight["x_dir = 1<br/>img_x = img_x - 1"]
CheckRight --> |否| MoveRight["img_x = img_x + 1"]
CheckLeft --> |是| ReverseLeft["x_dir = 0<br/>img_x = img_x + 1"]
CheckLeft --> |否| MoveLeft["img_x = img_x - 1"]
MoveRight --> CheckDirection
MoveLeft --> CheckDirection
ReverseRight --> CheckDirection
ReverseLeft --> CheckDirection
```

**图表来源**
- [lcd_display.v:67-86](file://lcd_display.v#L67-L86)

### 对角移动模式

对角移动模式实现图像同时在水平和垂直方向上的移动，支持边界穿透。

```mermaid
flowchart TD
Start([开始对角移动]) --> CheckBoundary{"检查边界条件"}
CheckBoundary --> |左边界| WrapLeft["img_x = H_DISP - 1"]
CheckBoundary --> |上边界| WrapTop["img_y = V_DISP - 1"]
CheckBoundary --> |正常| NormalMove["img_x = img_x - 1<br/>img_y = img_y - 1"]
WrapLeft --> CheckBoundary
WrapTop --> CheckBoundary
NormalMove --> CheckBoundary
```

**图表来源**
- [lcd_display.v:61-65](file://lcd_display.v#L61-L65)

**章节来源**
- [lcd_display.v:60-123](file://lcd_display.v#L60-L123)

## 速度控制机制

### 时钟频率与速度关系

系统采用33.3MHz的像素时钟频率，通过计数器实现精确的速度控制：

- **时钟频率**：33,300,000 Hz
- **目标速度**：60 像素/秒
- **节拍周期**：33,300,000 ÷ 60 = 555,000 个时钟周期
- **计数器宽度**：20位 (0 到 555,000-1)

### 速度调节算法

```mermaid
flowchart TD
CalcSpeed["计算速度参数"] --> SetCounter["设置MOVE_CNT_MAX"]
SetCounter --> GenerateTick["生成节拍信号"]
GenerateTick --> ControlMotion["控制运动执行"]
CalcSpeed --> |高速| SetCounterHigh["MOVE_CNT_MAX = 100,000"]
CalcSpeed --> |低速| SetCounterLow["MOVE_CNT_MAX = 1,000,000"]
SetCounterHigh --> GenerateTick
SetCounterLow --> GenerateTick
```

**图表来源**
- [lcd_display.v:26-46](file://lcd_display.v#L26-L46)

**章节来源**
- [lcd_display.v:26-46](file://lcd_display.v#L26-L46)

## 边界处理逻辑

### 边界检测机制

系统实现了多种边界处理策略：

```mermaid
flowchart TD
PixelPos["获取像素坐标"] --> CheckImageArea{"检查是否在图像区域内"}
CheckImageArea --> |是| CalcAddress["计算ROM地址"]
CheckImageArea --> |否| ResetAddress["重置ROM地址"]
CalcAddress --> AddressValid{"地址是否有效?"}
AddressValid --> |是| LoadPixel["加载像素数据"]
AddressValid --> |否| ResetAddress
LoadPixel --> OutputPixel["输出像素数据"]
ResetAddress --> OutputBackground["输出背景色"]
```

**图表来源**
- [lcd_display.v:134-186](file://lcd_display.v#L134-L186)

### 边界反弹算法

水平移动模式中的边界反弹逻辑：

```mermaid
flowchart TD
Start([检测边界]) --> CheckDir{"当前方向"}
CheckDir --> |向右| CheckRightEdge{"img_x >= H_DISP - WIDTH?"}
CheckDir --> |向左| CheckLeftEdge{"img_x <= 0?"}
CheckRightEdge --> |是| ChangeToRight["x_dir = 1<br/>img_x = img_x - 1"]
CheckRightEdge --> |否| MoveRight["img_x = img_x + 1"]
CheckLeftEdge --> |是| ChangeToLeft["x_dir = 0<br/>img_x = img_x + 1"]
CheckLeftEdge --> |否| MoveLeft["img_x = img_x - 1"]
ChangeToRight --> End([完成])
MoveRight --> End
ChangeToLeft --> End
MoveLeft --> End
```

**图表来源**
- [lcd_display.v:69-83](file://lcd_display.v#L69-L83)

**章节来源**
- [lcd_display.v:134-186](file://lcd_display.v#L134-L186)

## 拨码开关输入处理

### 输入优先级控制

系统通过4位拨码开关实现模式选择，采用优先级编码方式：

```mermaid
flowchart TD
Start([读取拨码开关]) --> ReadInputs["读取B0,B1,B2,B3"]
ReadInputs --> CheckMode{"检查模式组合"}
CheckMode --> |B3=1| DiagonalMode["对角移动模式"]
CheckMode --> |B2=1| HorizontalMode["水平移动模式"]
CheckMode --> |B1=1| VerticalMode["垂直振荡模式"]
CheckMode --> |B0=1| StaticMode["静态显示模式"]
CheckMode --> |默认| StaticMode
DiagonalMode --> SetParams["设置对角移动参数"]
HorizontalMode --> SetParams
VerticalMode --> SetParams
StaticMode --> SetParams
SetParams --> ApplyMode["应用运动参数"]
ApplyMode --> End([完成])
```

**图表来源**
- [lcd_display.v:60-123](file://lcd_display.v#L60-L123)

### 模式切换逻辑

```mermaid
flowchart TD
DetectSwitch["检测拨码开关变化"] --> ComparePrev{"与之前模式相同?"}
ComparePrev --> |是| NoChange["保持当前模式"]
ComparePrev --> |否| ModeChange["模式切换"]
ModeChange --> ResetParams["重置运动参数"]
ResetParams --> ApplyNewMode["应用新模式"]
ApplyNewMode --> UpdatePrev["更新先前模式"]
NoChange --> End([结束])
UpdatePrev --> End
```

**图表来源**
- [lcd_display.v:109-123](file://lcd_display.v#L109-L123)

**章节来源**
- [lcd_display.v:60-123](file://lcd_display.v#L60-L123)

## 定时器配置与坐标变换

### LCD时序配置

系统采用标准VGA时序参数：

```mermaid
classDiagram
class LcdTimingParameters {
+H_SYNC = 46
+H_BACK = 0
+H_DISP = 800
+H_FRONT = 210
+H_TOTAL = 1056
+V_SYNC = 23
+V_BACK = 0
+V_DISP = 480
+V_FRONT = 22
+V_TOTAL = 525
+calcPixelPos() coordinate
+calcEnableSignal() enable
}
class CoordinateCalculator {
+reg cnt_h,cnt_v
+assign pixel_xpos
+assign pixel_ypos
+assign data_req
}
LcdTimingParameters --> CoordinateCalculator
```

**图表来源**
- [lcd_driver.v:21-31](file://lcd_driver.v#L21-L31)
- [lcd_driver.v:67-69](file://lcd_driver.v#L67-L69)

### 坐标变换过程

```mermaid
flowchart TD
PixelClock["像素时钟"] --> HCounter["行计数器"]
PixelClock --> VCounter["场计数器"]
HCounter --> HSync["行同步信号"]
VCounter --> VSync["场同步信号"]
HSync --> EnableCalc["计算有效显示区域"]
VSync --> EnableCalc
EnableCalc --> PixelPosCalc["计算像素坐标"]
PixelPosCalc --> OutputPos["输出坐标"]
OutputPos --> ImageAreaCheck["检查图像区域"]
ImageAreaCheck --> |在区域内| RomAddressCalc["计算ROM地址"]
ImageAreaCheck --> |不在区域内| BackgroundOutput["输出背景色"]
```

**图表来源**
- [lcd_driver.v:72-94](file://lcd_driver.v#L72-L94)

**章节来源**
- [lcd_driver.v:21-94](file://lcd_driver.v#L21-L94)

## 状态机设计与时序分析

### 运动控制状态机

```mermaid
stateDiagram-v2
[*] --> 初始化
初始化 --> 静态显示 : B0=1
初始化 --> 水平移动 : B2=1
初始化 --> 垂直振荡 : B1=1
初始化 --> 对角移动 : B3=1
静态显示 --> 静态显示 : 保持
静态显示 --> 水平移动 : 模式切换
水平移动 --> 水平移动 : 继续移动
水平移动 --> 反弹处理 : 到达边界
垂直振荡 --> 垂直振荡 : 继续振荡
垂直振荡 --> 方向反转 : 到达130像素
对角移动 --> 对角移动 : 继续移动
对角移动 --> 边界处理 : 到达边界
反弹处理 --> 水平移动 : 继续
方向反转 --> 垂直振荡 : 继续
边界处理 --> 对角移动 : 继续
```

### 时序分析

系统采用流水线设计，确保像素数据的正确传输：

```mermaid
sequenceDiagram
participant Clock as 像素时钟
participant Driver as LCD驱动器
participant Display as 显示控制器
participant ROM as 图像存储器
Clock->>Driver : 时钟上升沿
Driver->>Driver : 更新计数器
Driver->>Display : 输出像素坐标
Display->>Display : 检查图像区域
Display->>ROM : 生成ROM地址
ROM->>Display : 返回像素数据
Display->>Driver : 输出像素数据
Driver->>Driver : 传输到LCD
Note over Clock,Driver : 时序关键点：坐标提前一个时钟周期
```

**图表来源**
- [lcd_driver.v:63-69](file://lcd_driver.v#L63-L69)

**章节来源**
- [lcd_display.v:137-152](file://lcd_display.v#L137-L152)
- [lcd_driver.v:63-69](file://lcd_driver.v#L63-L69)

## 性能优化建议

### 内存访问优化

1. **地址重置策略**：当图像位置改变时重置ROM地址，避免无效访问
2. **条件访问**：仅在图像区域内进行ROM访问，提高效率
3. **流水线设计**：利用ROM的单周期延迟特性，实现数据流水线

### 时钟域优化

1. **PLL配置**：使用ALTERA IP核实现精确的时钟频率合成
2. **时序裕量**：确保数据传输满足建立和保持时间要求
3. **时钟分频**：合理配置时钟分频比，平衡性能和功耗

### 代码结构优化

1. **参数化设计**：使用parameter和localparam统一管理常量
2. **模块化设计**：清晰的功能模块划分，便于维护和扩展
3. **注释规范**：详细的代码注释，便于理解和调试

## 故障排除指南

### 常见问题诊断

1. **图像不显示**
   - 检查拨码开关设置是否正确
   - 验证ROM文件加载是否成功
   - 确认时钟信号是否正常

2. **运动异常**
   - 检查运动参数配置
   - 验证边界处理逻辑
   - 确认时序是否正确

3. **显示闪烁**
   - 检查时钟频率配置
   - 验证同步信号生成
   - 确认像素数据传输时序

### 调试工具使用

1. **仿真验证**：使用ModelSim进行功能仿真
2. **时序分析**：利用Quartus进行时序约束分析
3. **硬件调试**：通过JTAG接口进行在线调试

### 性能监控

1. **资源利用率**：监控FPGA资源使用情况
2. **时钟频率**：验证实际工作频率
3. **功耗分析**：评估系统功耗表现

---

**章节来源**
- [lcd_display.v:137-152](file://lcd_display.v#L137-L152)
- [lcd_driver.v:63-69](file://lcd_driver.v#L63-L69)